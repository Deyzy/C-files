#include <stdio.h>

int main(){

    //operações aritmeticas 

    // ++ incremente 
    // -- decrement 

    int x = 5;
    int y = 2;

    // int a = x + y;
    // printf ("a :%d\n", a);

    // int b = x - y;
    // printf ("b :%d\n", b);

    // int c = x * y;
    // printf ("c :%d\n", c);

    // float d = x / (float)y;
    // printf ("d :%f\n", d);

    // int e = x % y;
    // printf ("e :%d\n", e);

    x++;
    y--;

    printf("%d\n", x);
    printf("%d\n", y);

    return 0;
}